name: Build

on:
  push:
    branches:
      - main
  pull_request:
  schedule:
    - cron: "0 0 * * 6" # every saturday at midnight

jobs:
  plugin_test:
    name: asdf plugin test
    strategy:
      matrix:
        # See https://github.com/actions/virtual-environments#available-environments
        os:
          - ubuntu-latest
          - macos-latest
    runs-on: ${{ matrix.os }}
    steps:
      - name: set up Java 11
        uses: actions/setup-java@3f07048e3d294f56e9b90ac5ea2c6f74e9ad0f98 # v3.10.0
        with:
          distribution: 'temurin'
          java-version: '11'
      - name: asdf_plugin_test
        uses: asdf-vm/actions/plugin-test@2368b9def7a1194d67ae0afd9cbbd01e15f658a0 # v2.0.0
        with:
          command: quarkus --help
