name: Build

on:
  push:
    branches:
      - main
  pull_request:
  schedule:
    - cron: "0 0 * * 6" # every saturday at midnight

jobs:
  plugin_test:
    name: asdf plugin test
    strategy:
      matrix:
        # See https://github.com/actions/virtual-environments#available-environments
        os:
          - ubuntu-latest
          - macos-latest
    runs-on: ${{ matrix.os }}
    steps:
      - name: set up Java 11
        uses: actions/setup-java@c3ac5dd0ed8db40fedb61c32fbe677e6b355e94c
        with:
          distribution: 'temurin'
          java-version: '11'
      - name: asdf_plugin_test
        uses: asdf-vm/actions/plugin-test@707e84f3ee349548310aeabdad0dd3bfcb9b69fa
        with:
          command: quarkus --help
